version: '3.8'

services:
  backend:
    image: wellness-core-backend
    build:
      context: .
      dockerfile: apps/wellness-core-backend/Dockerfile
    ports:
      - "9100:9100"
    networks:
      - platform-network

  admin:
    image: wellness-admin
    build:
      context: .
      dockerfile: apps/wellness-admin/Dockerfile
    ports:
      - "3001:80" # Map host 3001 to nginx internal 80
    networks:
      - platform-network

networks:
  platform-network:
    driver: bridge
